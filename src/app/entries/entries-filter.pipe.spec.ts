import { EntriesFilterPipe } from './entries-filter.pipe';

describe('EntriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
